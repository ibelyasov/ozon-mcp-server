{
  "name": "ozon-mcp-server-page",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:page": "node scripts/build-page.mjs",
    "check:page": "node scripts/build-page.mjs --check",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
